name: CI

on:
  pull_request:
  push:
    branches: [main, develop]

jobs:
  base-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run base quality check
        run: bash scripts/quality/check.sh
